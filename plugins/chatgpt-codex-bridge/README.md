# Codex App Chat Bridge skill

This plugin contains only the `bridge-chat` workflow. It talks to the separately installed Loader renderer package through `CodexScriptLoader.Command.exe`; it registers no MCP server.
